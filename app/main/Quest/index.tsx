import { Text } from "@/components";
import { colors } from "@/constants";
import React from "react";
import { View } from "react-native";

const Quest = () => {
  return (
    <View>
      <Text text="Quest" color={colors.black} />
    </View>
  );
};

export default Quest;
