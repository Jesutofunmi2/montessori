import { Text } from "@/components";
import { colors } from "@/constants";
import React from "react";
import { View } from "react-native";

const Profile = () => {
  return (
    <View>
      <Text text="Profile" color={colors.black} />
    </View>
  );
};

export default Profile;
