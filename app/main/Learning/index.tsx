import { Text } from "@/components";
import { colors } from "@/constants";
import React from "react";
import { View } from "react-native";

const Learning = () => {
  return (
    <View>
      <Text text="Learning" color={colors.black} />
    </View>
  );
};

export default Learning;
